(function($) {

	/**
	 * This plugin provides a timer interface.
	 *
	 * @author: Nils Hörrmann, post@nilshoerrmann.de
	 * @source: http://github.com/nilshoerrmann/timer
	 */
	$.fn.symphonyTimer = function() {
		var objects = this;

	/*---- Events -----------------------------------------------------------*/

		objects.delegate('div.calendar', 'set.calendar', function(event) {
		
		});

	/*---- Functions --------------------------------------------------------*/
	
		// Visualise time
		var visualise = function() {
		
		};

	};
		
})(jQuery.noConflict());
